import {View} from 'react-native';

// Cpmponents
import TextElement from '../components/resuable/TextElement';

const SearchScreen = () => {
  return (
    <View>
      <TextElement>Search Screen</TextElement>
    </View>
  );
};

export default SearchScreen;
